export enum StatementAtom {
    verify = 'verify',
    ifBlock = 'ifBlock',
    elseBlock = 'elseBlock',
    forGroup = 'forGroup',
    whileGroup = 'whileBlock',
    set = 'set',
    defineLocal = 'defineLocal',
    result = 'result',
}

export default StatementAtom;
